import { dpInverse } from 'dummy/helpers/dp-inverse';
import { module, test } from 'qunit';

module('Unit | Helper | dp inverse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dpInverse([42]);
  assert.ok(result);
});
