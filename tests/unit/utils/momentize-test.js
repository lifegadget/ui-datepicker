import momentize from 'dummy/utils/momentize';
import { module, test } from 'qunit';

module('Unit | Utility | momentize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentize();
  assert.ok(result);
});
