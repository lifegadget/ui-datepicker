import containerFormat from 'dummy/utils/container-format';
import { module, test } from 'qunit';

module('Unit | Utility | container format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = containerFormat();
  assert.ok(result);
});
