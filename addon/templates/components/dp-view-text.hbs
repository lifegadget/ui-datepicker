<view
  id={{elementId}}
  class="view-text {{class}}"
  onclick={{action 'toggleInputs'}}
>
  {{#if hasBlock}}

    {{yield}}

  {{else}}

    {{#if (and datetime format)}}
      {{moment-format datetime format}}
    {{/if}}

  {{/if}}
</view>
