<incrementer
  id={{elementId}}
  class="{{class}}"

  onclick={{action 'onIncrement' target increment incrementUnit}}
>

  {{#if hasBlock}}
    {{yield}}
  {{else}}
    <img class='increment-image' src="/ui-datepicker/images/{{direction}}.svg" alt="">
  {{/if}}

</incrementer>
