<incrementer
  id={{elementId}}
  class="{{class}}"
>

  {{#if hasBlock}}
    {{yield}}
  {{else}}
    <img class='increment-image' src="/ui-datepicker/images/{{direction}}.svg" alt="">
  {{/if}}

</incrementer>
