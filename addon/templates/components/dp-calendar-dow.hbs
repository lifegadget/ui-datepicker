<dow class="{{if class (concat class ' ')}}days-of-week flex fill justify-center">
  {{#if hasBlock}}
    {{yield}}
  {{else}}

    {{#each _dow as |day|}}
      <span class="day {{day.type}}-heading flex fill">
        {{day.name}}
      </span>
    {{/each}}

  {{/if}}
</dow>
